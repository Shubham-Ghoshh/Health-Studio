import 'package:flutter/material.dart';

class BottomNavItem {
  String label;
  String iconPath;
  IconData iconData;

  BottomNavItem({
    required this.label,
    required this.iconPath,
    required this.iconData,
  });
}
